language: python
os:
  - linux
python:
  - "2.7"
  - "3.5"
jdk:
  - oraclejdk8
script:
  - pip install coverage
  - pip install nose
  - python -m nltk.downloader all
  - nosetests --with-coverage --cover-package=tethne
